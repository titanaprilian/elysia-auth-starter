import { z } from "zod";

/**
 * Schema for creating {{capitalize name}}
 */
export const Create{{capitalize name}}Schema = z.object({
  // TODO: define required fields
});

/**
 * Schema for updating {{capitalize name}}
 * Intentionally excludes sensitive fields (e.g. password)
 */
export const Update{{capitalize name}}Schema = z.object({
  // TODO: define updatable fields
});

/**
 * Inferred types
 */
export type Create{{capitalize name}}Input =
  z.infer<typeof Create{{capitalize name}}Schema>;

export type Update{{capitalize name}}Input =
  z.infer<typeof Update{{capitalize name}}Schema>;
